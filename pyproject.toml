[project]
name = "h35-desktop-workspace"
version = "0.0.0"
description = "h35-desktop repository operator workspace."
requires-python = ">=3.12"
dependencies = ["h35-ops"]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["h35-ops"]

[tool.uv.sources]
h35-ops = { workspace = true }
